name: akashi_gateway
description: >-
  Model routing and provider fallback for the Akashi agent framework: resolve
  "provider/model" strings and build resilient FallbackModel chains behind one
  LanguageModel contract.
version: 0.3.0
repository: https://github.com/AleSZanello/akashi_agents/tree/main/packages/akashi_gateway
issue_tracker: https://github.com/AleSZanello/akashi_agents/issues
topics:
  - ai
  - agents
  - llm

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  akashi: ^0.3.0

dev_dependencies:
  test: ^1.25.0
