name: akashi_gateway
description: >-
  Model routing and provider fallback for the Akashi agent framework. Resolve
  "provider/model" strings via a ProviderRegistry and build resilient
  FallbackModel chains, all behind Akashi's LanguageModel contract.
version: 0.2.0
repository: https://github.com/Alezanello/akashi_agents/tree/main/packages/akashi_gateway
issue_tracker: https://github.com/Alezanello/akashi_agents/issues
topics:
  - ai
  - agents
  - llm

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  akashi: ^0.2.0

dev_dependencies:
  test: ^1.25.0
